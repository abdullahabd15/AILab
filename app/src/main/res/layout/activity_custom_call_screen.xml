<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".custom_call_screen.CustomCallScreenActivity">

    <TextView
        android:id="@+id/tv_caller_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/img_telemarketer"
        android:gravity="center"
        android:text="Caller ID"
        style="@style/TextAppearance.AppCompat.Large"/>

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@id/img_telemarketer"
        android:visibility="gone"/>

    <ImageView
        android:id="@+id/img_telemarketer"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginTop="20dp"
        android:layout_centerInParent="true"
        android:src="@drawable/ic_person"
        android:tint="@color/colorPrimary"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:paddingTop="30dp"
        android:paddingBottom="30dp"
        android:layout_marginBottom="50dp"
        android:orientation="horizontal"
        android:gravity="center">

        <ImageView
            android:id="@+id/btn_reject"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:src="@mipmap/ic_call_end"/>

        <ImageView
            android:id="@+id/btn_pick_up"
            android:layout_width="70dp"
            android:layout_height="70dp"
            android:layout_marginStart="60dp"
            android:src="@mipmap/ic_call"
            android:visibility="gone"/>

    </LinearLayout>


</RelativeLayout>